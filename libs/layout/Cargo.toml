[package]
name = "grids-layout"
version = "0.1.0"
edition = "2021"
description = "Grid-based page layout engine for print and screen"

[lib]
name = "grids_layout"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
