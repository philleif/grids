#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
# Use query_main entry point
exec "$SCRIPT_DIR/.venv/bin/python" -c "from grids.knowledge.store import query_main; query_main()" -- "$@"
